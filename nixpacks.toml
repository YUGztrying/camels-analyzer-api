[phases.setup]
nixPkgs = ["...", "tesseract", "poppler_utils"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python init_db.py && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
